class User {
  String username;
  String email;
  String password;
  String bloodtype;
  double latitude;
  double longitude;
  String imageURL;

  User(this.username, this.email, this.password, this.bloodtype, this.latitude,
      this.longitude, this.imageURL);
}
